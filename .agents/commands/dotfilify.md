---
description: Move config into dotfiles with optional private overlay fragments
---

Use the `dotfilify` skill to move config into this dotfiles repo and wire the Home Manager merge. Additional user instructions: $ARGUMENTS
