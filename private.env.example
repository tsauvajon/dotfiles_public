# Copy to ~/.config/dotfiles/private.env and fill in real values.

# Git identity
GIT_NAME="Your Full Name"
GIT_EMAIL="you@example.com"
GIT_SIGNINGKEY="0000000000000000"  # 16-char hex key ID — run: gpg --list-secret-keys --keyid-format long

# goto shortcut manager API endpoint
# See: https://github.com/tsauvajon/goto
GOTO_CONFIG_API_URL="http://your-host:50002"  # used by ~/.config/goto/config.yml

# VSCodium trusted workspace roots (space-separated list of absolute paths)
# These are injected into ~/.config/task/config.toml under [vscodium].trusted_roots
VSCODIUM_TRUSTED_ROOTS="/home/youruser/dev/wt/github.com/youruser /home/youruser/dev/repos/github.com/youruser"
